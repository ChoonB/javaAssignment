문제 : Calculator 클래스가 4가지 연산의 책임을 다 지고 있어 책임이 너무크다.
그래서 각각의 연산마다 클래스를 새로 만들어야 한다.

해결 : Calculator를 추상클래스로 만들고 
operate 추상메서드로 만든다.
각각의 연산 클래스를 만들어 Calculator를 상속받아
미완성인 operate 메서드를 각자의 연산으로 완성시킨다.
Client에서는 각각의 연산마다 인스턴스를 만들고
각연산의인스턴스.operate()를 통해 각각의 연산을 가능하게 했다.

이로써 SRP에 위배되지 않게 되었다.